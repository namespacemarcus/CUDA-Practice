# torch with CUDA support — install the build matching your CUDA toolkit
# from https://pytorch.org, e.g.:
#   pip install torch --index-url https://download.pytorch.org/whl/cu126
torch>=2.1

ninja

pytest>=7.0
pre-commit
